@using BookKeeper.Application.Books.GetBooks
@model BookResponse

<div class="col">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">@Model.Title by @string.Join(", ", Model.Authors)</h5>
            <a href="#" class="btn btn-primary">View</a>
        </div>
    </div>
</div>